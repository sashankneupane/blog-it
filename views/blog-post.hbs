<link rel="stylesheet" href="/styles/blog-post.css">
{{#with blogPost}}
<div class="blog-post">
    <h2>{{title}}</h2>
    <p class="author">Written by <a href="/u/{{author.username}}">{{author.username}}</a></p>
    <p class="date-published">Published on {{publicationDate}}</p>
    {{#if updatedDate}}
        <p class="date-updated">Updated on {{updatedDate}}</p>
    {{/if}}
    <p class="content">{{content}}</p>
    
    {{likes.length}} <img src="/img/like.png" height="20px" width="20px"> {{comments.length}} <img src="/img/comment.png" height="20px" width="20px">

    {{#if comments.length}}
        <ul class="comment-list">
            {{#each comments}}
                <li class="comment">
                    <div class="comment-header">
                        <p class="comment-author"><a href="/u/{{author.username}}">{{author.username}}</a></p>
                    </div>
                    <p class="comment-text">{{text}}</p>
                </li>
            {{/each}}
        </ul>
    {{/if}}
</div>
{{/with}}